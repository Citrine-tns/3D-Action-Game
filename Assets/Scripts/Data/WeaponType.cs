/// <summary>
/// 武器種別
/// </summary>
public enum WeaponType
{
    Greatsword, // 大剣（斬）
    Dagger,     // 短剣（斬）
    Hammer,     // 槌（壊）
    Club,      // 棍（壊）
    Spear,      // 槍（突）
    Gun         // 銃（突）
}
